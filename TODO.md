# TODO

3 ペルソナ (技術ライター / 校正用途の編集者 / 要約用途の学生) によるブラウザテスト
(2026-08-27) で挙がった課題。上から順に対応する。チェックは完了したもの。

## フェーズ 1: バグ修正 (実害あり)

- [ ] **1. Context 変更・手修正後の再翻訳がキャッシュに食われる** — `app/useRowTranslation.ts`
  - キャッシュキーに context のハッシュを追加
  - `retranslate(ids)` は明示的な再実行としてキャッシュを読まずに API へ。結果はキャッシュに上書き
- [ ] **2. 右クリック「再翻訳」が全選択行に効く** — `app/grid/Grid.tsx`
  - メニューを `再翻訳 (N 行)` と行数つきにし、複数行選択時は `この行だけ再翻訳` を追加
- [ ] **3. Context 自動生成の勝手な消費 / チェックが勝手に OFF になる** — `app/pages/Translate.tsx`, `app/useAutoContext.ts`, `app/domain.ts`
  - モーダルを開いた時点の値と draft を比較し、実際に書き換えた時だけ自動生成を OFF
  - 自動生成の完了時にトーストで消費を可視化
  - Context 生成プロンプトに「原文と同じ言語で」を追加 (日本語校正で英語 Context が入る問題)
- [ ] **4. 文章長スライダーが弱い / Prompt と競合** — `app/domain/style.ts`
  - 短縮側を数値で縛る。独自 Prompt 時は Prompt の制約 (文数など) を優先すると明記
- [ ] **5. 文章長ドラッグ中のプレビューが出ない (要実機確認)** — `app/components/StylePanel.tsx`, `app/pages/Translate.tsx`
  - `draggingLength` を ref 化して即時判定にする

## フェーズ 2: 翻訳以外の用途向けのラベル・導線

- [ ] **6. カスタム Prompt 時のラベル切り替え** — 列見出し「訳文→出力」、「再翻訳→再生成」、パネル説明・トースト
- [ ] **7. Prompt 保存時に「保存して全行を再生成 (N 行・約 M cr)」ボタン**
- [ ] **8. Prompt プリセット** (翻訳 / 校正 / 1 文要約 / 言い換え) と前回 Prompt の候補表示
- [ ] **9. 大量貼り付けの確認** — 自動翻訳対象が閾値を超えたら即実行せず、ツールバーのボタンで実行

## フェーズ 3: 品質・後回しでよいもの

- [ ] 10. スライダー再処理を undo 単位に (`retranslate` の訳文消去を `commit` に)
- [ ] 11. 選択変更時のスライダーリセットを可視化
- [ ] 12. 「Settings」(GitHub リンクのみ) をユーザーメニューへ移し、ヘッダーは「ノート設定」だけに
- [ ] 13. 独自 Prompt 時はノート一覧のタイトルに右カラム 1 行目を使う
- [ ] 14. 校正向け diff ハイライト (同一言語の独自 Prompt 時のみ)
- [ ] 15. 右クリックの ChatGPT/Claude 連携文言を Prompt モードに合わせる

## テストで確認できた良い点 (維持する)

- 空行区切りの貼り付けで段落が行になる。Enter で次行、最終行 Enter で空行追加
- 独自 Prompt で校正・要約が翻訳されずに動く。リロード後も保持
- 手修正した行のオレンジマーカー。明示的な再翻訳でのみ上書き
- 列幅ドラッグ、Cmd+Z で貼り付けをまとめて取り消し
